---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "am Provider"
description: |-
  Gravitee: Gravitee Access Management Terraform Provider (alpha)
  You can manage with Terraform the following:
  DomainsCertificatesIdentity ProvidersReporters
  Compatible with Gravitee Access Management 4.12.0.
---

# am Provider

Gravitee: Gravitee Access Management Terraform Provider (alpha)

You can manage with Terraform the following:
* Domains
* Certificates
* Identity Providers
* Reporters

Compatible with Gravitee Access Management 4.12.0.

## Example Usage

```terraform
terraform {
  required_providers {
    am = {
      source  = "gravitee-io/am"
      version = "0.0.1"
    }
  }
}

provider "am" {
  server_url = "..." # Optional - can use AM_SERVER_URL environment variable
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `bearer_auth` (String, Sensitive) Authentication uses a bearer token: a JWT, or an opaque user service-account access token. Every operation is additionally permission-gated against the target organization, environment, and resource; a caller lacking the required permission receives a 403 response. Configurable via environment variable `AM_SA_TOKEN`.
- `environment_id` (String) Identifier of the environment.
- `http_headers` (Map of String) HTTP headers to include in all requests
- `organization_id` (String) Identifier of the organization that owns the environment.
- `server_url` (String) Server URL (defaults to http://localhost:8093/automation)
